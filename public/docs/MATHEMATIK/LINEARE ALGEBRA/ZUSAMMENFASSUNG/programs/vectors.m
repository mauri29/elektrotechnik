a = [1; 2; 3] %Vektor a
b = [4; 5; 6] %Vektor b
c = [7; 8; 9] %Vektor c

s = a + b; %Summe zweier Vektoren
d = a - b; %Differenz zweier Vektoren

e = norm(a); %Betrag des Vektors

lambda = 7; %Parameter
f = lambda*a; %parametrisierter Vektor
n = a/e %normierter Vektor a

m = a+(1/2)*(b-a); %Mittelpunkt zweier Vektoren
g = (1/3)*(a+b+c); %Schwerpunkt dreier Vektoren
